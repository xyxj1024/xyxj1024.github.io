---
layout: 		post
title: 			"Binary Search Tree in Standard ML"
category:		"Programming Languages"
tags:			data-structures functional-programming
permalink:		/bst-sml/
---

In this post, I would like to describe a Standard ML implementation of **binary search tree (BST)**. Each tree **node**, if not empty, records information about its own **value** along with its **descendant(s)**. A client should be able to (a) <pre>find</pre> a node value using a **key**; (b) <pre>insert</pre> values onto an existed tree; (c) <pre>remove</pre> nodes from an existed tree; (d) perform tree traversal using higher order functions. The BST is kept as general as possible such that the values and keys can be any types.

<!-- excerpt-end -->

<br />
## Table of Contents
{:.no_toc}
* TOC 
{:toc}
<br />

```Standard ML
datatype order = LESS | EQUAL | GREATER

type 'k compare_function = (('k * 'k) -> order)
type ('e,'k) to_key_function = 'e -> 'k

datatype 'e node = Nil
	             | Sub of 'e * ('e node) * ('e node)
type ('e,'k) tree = ('e node * 'k compare_function * ('e,'k) to_key_function)
```